// Home page — entry point for students joining a session via PIN code.
'use client';

import { HomeJoin } from '@/components/shared/home/HomeJoin';

export default function Home() {
  return <HomeJoin />;
}
